name: Fugue CI/CD Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci
      - run: npm ci --prefix client

  build-dev:
    needs: test
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build dev image
        run: docker build -f Dockerfile.dev -t fugue-dev .

  build-and-deploy-prod:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for GCP
        run: gcloud auth configure-docker

      - name: Convert repository name to lowercase
        id: repo_name
        run: echo "repo_lowercase=$(echo ${{ github.event.repository.name }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build Docker image
        run: |
          docker build -f Dockerfile.prod \
            -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/${{ steps.repo_name.outputs.repo_lowercase }}:${{ github.sha }} \
            -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/${{ steps.repo_name.outputs.repo_lowercase }}:latest \
            .

      - name: Push Docker image to GCR
        run: |
          docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/${{ steps.repo_name.outputs.repo_lowercase }}:${{ github.sha }}
          docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/${{ steps.repo_name.outputs.repo_lowercase }}:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ steps.repo_name.outputs.repo_lowercase }} \
            --image gcr.io/${{ secrets.GCP_PROJECT_ID }}/${{ steps.repo_name.outputs.repo_lowercase }}:${{ github.sha }} \
            --platform managed \
            --region ${{ secrets.GCP_REGION }} \
            --allow-unauthenticated \
            --port 3000 \
            --memory 512Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 10

      - name: Get Service URL
        run: |
          echo "Service URL:"
          gcloud run services describe ${{ steps.repo_name.outputs.repo_lowercase }} \
            --platform managed \
            --region ${{ secrets.GCP_REGION }} \
            --format 'value(status.url)'